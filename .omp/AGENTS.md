# OMP project context

Use the repository's existing project guidance and fork-specific rules as one source of truth. The import below expands `CLAUDE.md`, which in turn imports `AGENTS.md`; do not duplicate those policies here.

@../CLAUDE.md
