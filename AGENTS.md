# peepie 二次开发规范

peepie 是基于 PentAGI（`vxcontrol/pentagi`）的二开项目，GitHub 仓库为 `zerx-lab/peepie`。现有 `pentagi` 包名、服务名、镜像名和 API 字段仍是上游技术契约；未经专门迁移任务，不因产品命名而批量改名。以下规则适用于 AI 助手和人工开发者；产品改动以正确性和可持续同步上游为目标，不以“零冲突”替代正确实现。架构、构建命令及密码策略见 `CLAUDE.md`，依赖许可证要求见 `CONTRIBUTING.md`。

## 仓库与分支

- `origin` 指向 `zerx-lab/peepie`；`upstream` 指向 `vxcontrol/pentagi` 且只用于获取更新，禁止向它 push。保留 fork 默认分支 `main`，产品名无需映射成分支名。不要把二开代码直接提交到上游跟踪分支，也不要擅自推送、强推、重置、改写共享分支历史。
- 新克隆仓库需自行配置只读上游：`git remote add upstream https://github.com/vxcontrol/pentagi.git`，再执行 `git remote set-url --push upstream DISABLED`；先核实官方默认分支，不预设分支名。本地 Git remote 配置不会随代码提交传播。
- 从 fork 的集成分支切功能分支；一个需求保持独立提交。功能、重构、格式化、依赖升级和上游同步不要混在同一提交中。
- 同步前先获取上游默认分支及 fork 集成分支的最新状态，核实目标分支、工作区改动和双方差异。共享分支合并上游；仅未共享的个人分支可 rebase。不得覆盖他人未提交的修改。
- 冲突解决后检查行为变化，不只检查 Git 冲突标记。记录上游版本、冲突文件、选择保留/适配的行为及验证结果；无法安全合并时停止并说明阻碍，不通过丢弃任一方实现来“解决”冲突。
- 上游 CI 中的 Docker 发布目标仍是 `vxcontrol/pentagi`；fork 不得发布到这个镜像。为 peepie 发布镜像前，需另行确定仓库、凭据、版本与部署配置，不能直接复用上游发布任务。

## 改动边界

- 优先沿现有扩展点新增文件，并以少量明确的入口接入；必须修改上游核心逻辑时直接做最小、可解释的改动，不引入脆弱的覆盖、猴子补丁或构建期全局字符串替换。
- 修改前读相关调用方、测试和数据契约；修改后同步迁移所有受影响调用方。避免无关重排、全项目自动格式化和顺手重构，以减少以后同步时的重叠。
- 数据库变更使用现有迁移机制；GraphQL、Swagger、前端 GraphQL 类型按 `CLAUDE.md` 中的命令生成。不要手改生成文件，也不要仅修改生成物来掩盖源文件问题。
- 对外协议、鉴权、权限、审计日志、工具执行隔离和错误码保持兼容；确需变更时同时更新调用方、迁移与行为测试。密码相关改动遵循 `CLAUDE.md` 的前后端一致策略。
- 不提交密钥、令牌、真实攻击目标数据或本地 `.env`；新增依赖先核对 `CONTRIBUTING.md` 的许可证要求。不得因方便而削弱安全校验或沙箱边界。

## 中英文支持约定

- 界面固定文案、日期数字格式、可访问性文案和用户可见错误消息是国际化范围；优先在集中词条和语言配置中维护，英文作为缺失译文回退。不要用浏览器/DOM 自动翻译替代应用内国际化。
- 优先按稳定错误码翻译已知服务端错误，不把英文 `msg` 文本当作稳定标识；日志、API 字段、提示词、命令、用户输入和 Agent 生成内容不能作为界面文案批量翻译。
- 新增界面功能时同时考虑两种语言、动态参数、长文本布局和缺失译文；不为追求少改文件而采用不可验证的全量源码转换。报告/PDF 等生成内容若要求双语，应作为独立的产品需求明确范围。
- Web 前端按 `frontend/docs/i18n.md`（i18next 命名空间、类型化键、`i18next/no-literal-string` lint、词条一致性测试、中文术语表）实现；安装器 TUI 按 `backend/docs/installer_i18n.md`（`locale.go` 英文 `var` + `catalog_zh_cn_*.go` 中文词条、`TestEveryTextIsTranslated`）实现。新增或修改界面文案必须在同一变更中补齐全部语言。
- 同步上游后，上游新增的前端硬编码文案会被 lint 拦截、新增的安装器文案会被 `TestEveryTextIsTranslated` 拦截，需在合并时补齐迁移与翻译；上游新增的 REST 错误码同步到 `frontend/src/i18n/locales/*/apiErrors.json`。

## 交付与验证

- 每个变更说明动机、涉及的上游接口/文件、风险及验证；Bug 修复先复现，再验证修复。至少执行覆盖改动路径的确定性检查，例如相关 Go/Vitest 测试及受影响前后端构建；同步上游后还需跑受影响模块的回归检查。GUI 验证由用户明确提出时再进行。
- 文档或规则文件变动至少检查引用路径、Git diff 格式和规则入口是否可被目标工具加载。未运行的检查不得报告为通过；不可执行时说明具体原因。
- 上游新增功能与本地实现冲突时，优先评估能否采用上游实现并删除本地重复代码，而不是永久保留两套逻辑。
